# Módulo isolado — reaproveita o mesmo driver (PyMongo) da POC existente.
# Nenhuma dependência de Redis: o Redis é simulado in-process (shared/fake_redis.py).
pymongo==4.17.0
python-dotenv==1.2.2
fastapi==0.136.3
uvicorn==0.49.0
